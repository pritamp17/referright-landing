SITE_URL=https://www.rightrefer.com
PUBLIC_APP_SIGNUP_URL=https://app.rightrefer.com/
PUBLIC_CONTACT_EMAIL=rightrefer.team@gmail.com

# Hosted policy pages. The footer renders each link only once its URL is set,
# so an unset value shows nothing rather than a link to a page that is not
# there. Both are a launch requirement.
PUBLIC_PRIVACY_URL=
PUBLIC_TERMS_URL=
